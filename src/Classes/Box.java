package Classes;

public class Box {
    // this class will draw a box that is the appropriate size on the day view if the event occurs on that day
    // it will assign that box appropriate values depending on the event, most importantly the event ID
    // clicking the box will pass the event ID to an EventController and prompt the eventController window to open

}
